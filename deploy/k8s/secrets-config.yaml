apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: tutup-lapak
stringData:
  JWT_SECRET: change-me-in-prod
  INTERNAL_SECRET: backend-infra-internal-secret
---
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: tutup-lapak
stringData:
  # Example for single shared DB; rotate per service if needed
  DATABASE_URL: postgres://postgres:postgres@postgres.tutup-lapak.svc.cluster.local:5432/tutup_lapak?sslmode=disable
