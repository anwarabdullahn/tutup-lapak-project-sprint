{{- range $name, $svc := .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
spec:
  type: {{ $svc.service.type }}
  selector:
    app: {{ $name }}
  ports:
  - port: {{ $svc.service.port }}
    targetPort: {{ $svc.service.port }}
---
{{- end }}
