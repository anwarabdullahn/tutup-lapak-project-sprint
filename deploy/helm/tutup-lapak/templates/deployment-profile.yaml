{{- if .Values.services.profile.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: profile-service
spec:
  replicas: {{ .Values.services.profile.replicaCount }}
  selector:
    matchLabels:
      app: profile-service
  template:
    metadata:
      labels:
        app: profile-service
    spec:
      containers:
        - name: profile
          image: {{ include "tutup-lapak.image" (dict "imageName" "profile-service" "Values" .Values "Chart" .Chart) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.profile.port }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.services.profile.port | quote }}
            - name: DATABASE_URL
{{- if .Values.global.database.hardcode.enabled }}
              value: {{ .Values.global.database.hardcode.url | quote }}
{{- else }}
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DATABASE_URL
{{- end }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.profile.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.profile.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
{{- if .Values.services.profile.resources }}
{{ toYaml .Values.services.profile.resources | indent 12 }}
{{- else }}
{{ toYaml .Values.resources.default | indent 12 }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: profile-service
  ports:
    - port: {{ .Values.services.profile.port }}
      targetPort: {{ .Values.services.profile.port }}
{{- end }}
