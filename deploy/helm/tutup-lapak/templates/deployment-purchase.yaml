{{- if .Values.services.purchase.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchase-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: purchase-service
spec:
  replicas: {{ .Values.services.purchase.replicaCount }}
  selector:
    matchLabels:
      app: purchase-service
  template:
    metadata:
      labels:
        app: purchase-service
    spec:
      containers:
        - name: purchase
          image: {{ include "tutup-lapak.image" (dict "imageName" "purchase-service" "Values" .Values "Chart" .Chart) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.purchase.port }}
          env:
            - name: SERVER_PORT
              value: {{ .Values.services.purchase.port | quote }}
            - name: DATABASE_URL
{{- if .Values.global.database.hardcode.enabled }}
              value: {{ .Values.global.database.hardcode.url | quote }}
{{- else }}
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DATABASE_URL
{{- end }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
            - name: PRODUCT_SERVICE_URL
              value: "http://product-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ .Values.services.product.port }}"
            - name: USER_SERVICE_URL
              value: "http://auth-service.{{ .Values.global.namespace }}.svc.cluster.local:{{ .Values.services.auth.port }}"
            - name: INTERNAL_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: INTERNAL_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.purchase.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.purchase.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
{{- if .Values.services.purchase.resources }}
{{ toYaml .Values.services.purchase.resources | indent 12 }}
{{- else }}
{{ toYaml .Values.resources.default | indent 12 }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: purchase-service
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: purchase-service
  ports:
    - port: {{ .Values.services.purchase.port }}
      targetPort: {{ .Values.services.purchase.port }}
{{- end }}
